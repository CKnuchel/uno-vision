import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const primary = Color(0xFFFF3B3B);
  static const onPrimary = Color(0xFFFFFFFF);

  // Light Mode
  static const backgroundLight = Color(0xFFF5F5F5);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const surfaceVariantLight = Color(0xFFEFEFEF);
  static const textPrimaryLight = Color(0xFF1A1A1A);
  static const textSecondaryLight = Color(0xFF6B6B6B);

  // Dark Mode
  static const backgroundDark = Color(0xFF0F1117);
  static const surfaceDark = Color(0xFF1A1D2E);
  static const surfaceVariantDark = Color(0xFF252838);
  static const textPrimaryDark = Color(0xFFF5F5F5);
  static const textSecondaryDark = Color(0xFF9A9AB0);

  // Status
  static const success = Color(0xFF34C759);
  static const warning = Color(0xFFFF9500);
  static const error = Color(0xFFFF3B3B);

  // Shadows
  static const shadowLight = Color(0x15000000);
  static const shadowDark = Color(0x40000000);
}
