services:
  # Flutter Web App
  # Cloudflare Tunnel: uno-vision.domain.com -> localhost:3000
  web:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    restart: unless-stopped

  # Go API Backend
  # Cloudflare Tunnel: api.uno-vision.domain.com -> localhost:3001
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3001:8080"
    environment:
      - PORT=8080
      - DB_PATH=/app/data/sqlite.db
      - UPLOAD_PATH=/app/uploads
      - GIN_MODE=release
    volumes:
      - db_data:/app/data
      - uploads:/app/uploads
    restart: unless-stopped

volumes:
  db_data:
  uploads: